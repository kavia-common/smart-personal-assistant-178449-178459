pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "example-android-app"

include(":app")
include(":list")
include(":utilities")
