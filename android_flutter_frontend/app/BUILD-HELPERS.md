# Build Helpers

To build the Android app from within the app module, run Gradle from the Android project root:

- Project root: one level up (smart-personal-assistant-178449-178459/android_flutter_frontend)
- Wrapper path: ../gradlew

Examples:
  cd ..
  ./gradlew :app:assembleDebug
