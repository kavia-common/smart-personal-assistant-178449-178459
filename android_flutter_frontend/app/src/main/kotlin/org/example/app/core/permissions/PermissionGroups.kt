package org.example.app.core.permissions

/**
 * PUBLIC_INTERFACE
 * Canonical string constants representing permission groups for analytics/telemetry,
 * logging, or UI tags. Keeps magic strings out of the codebase.
 */
object PermissionGroups {
    const val LOCATION = "location"
    const val MEDIA = "media"
    const val SMS = "sms"
}
