This directory is the Gradle project root for the Android app.

- Uses Declarative Gradle DSL (settings.gradle.dcl and build.gradle.dcl)
- Wrapper: ./gradlew
- Primary module: :app
- Build with: ./gradlew build
```bash
# from repository root
./android_flutter_frontend/gradlew -p android_flutter_frontend build
```
