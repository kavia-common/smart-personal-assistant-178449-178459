pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "example-android-app"

include(":app")
include(":utilities")
include(":list")
