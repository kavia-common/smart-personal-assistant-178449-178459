This repository's actual Gradle wrapper is under android_flutter_frontend/gradle/wrapper/.

Tools that scan only the repository root should invoke:
  ./android_flutter_frontend/gradlew -p android_flutter_frontend <task>
