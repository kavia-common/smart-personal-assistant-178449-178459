# Root-level gradle.properties shim to aid analyzers scanning from repo root
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Delegate builds to module wrapper by default (informational)
android.moduleRoot=android_flutter_frontend
