plugins {
    // empty: this root delegates to the nested android project
}

gradle.beforeProject {
    logger.lifecycle("Delegating Gradle build to nested Android project under android_flutter_frontend")
}
