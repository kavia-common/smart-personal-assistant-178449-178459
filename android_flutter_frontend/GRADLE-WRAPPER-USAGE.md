# Gradle Wrapper Usage (Android Module)

Use the Gradle wrapper in this module to build the app:
- ./gradlew :app:assembleDebug
- ./gradlew :app:installDebug

This project uses Gradle 9 Declarative DSL (.dcl). The settings file is:
- settings.gradle.dcl
